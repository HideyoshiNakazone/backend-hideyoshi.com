# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      env:
        DATABASE_URL_PSQL: postgresql://ec2-34-224-226-38.compute-1.amazonaws.com/dfo2dcb89llp0v
        DATABASE_USER: rwpjcgoinhtvoz
        DATABASE_PASSWORD: fc374271bf94a8cfed2a92b4a9993a70a1183aca57291795236c0f85545c21fb
        FRONT_END_PATH: https://com-hideyoshi.herokuapp.com
      run: mvn -B package --file pom.xml
